## SECTION=test_suites

config MDBTEST
	tristate
	prompt "mdbtest"
	select LIBMDB
	help
	  This is a test tool for MDB. It attempts to connect to a cashless
	  device and sends configuration data to it. On success, the response
	  is printed on the standard output.
