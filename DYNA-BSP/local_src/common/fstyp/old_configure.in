AC_INIT(src/fstyp.c)

AM_CONFIG_HEADER(config.h)
AM_INIT_AUTOMAKE(fstyp,0.1)
AM_MAINTAINER_MODE

AC_ISC_POSIX
AC_PROG_CC
AC_STDC_HEADERS
AC_ARG_PROGRAM

AC_SUBST(CFLAGS)
AC_SUBST(CPPFLAGS)
AC_SUBST(LDFLAGS)

AM_PROG_LIBTOOL

AC_HEADER_STDC
AC_CHECK_HEADERS(fcntl.h)
AC_C_CONST
AC_CHECK_FUNCS(mkdir)

AC_OUTPUT([
	Makefile
	man/Makefile
	src/Makefile
	])
